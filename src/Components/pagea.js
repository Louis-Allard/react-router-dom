import React from 'react';
import Menu from "./menu"

const PageA = () => {
    return (
        <div>
            <Menu title="Menu de la première page" />
          <h1>PageA</h1>
        </div>
    );
};

export default PageA;